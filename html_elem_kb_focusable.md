# Setting an HTML element keyboard navigation focusable

Using attribute and value `tabindex="0"` to set an HTML element keyboard navigation focusable:

```html
<abbr tabindex="0" title="abbreviation">ABBR</abbr>
```
